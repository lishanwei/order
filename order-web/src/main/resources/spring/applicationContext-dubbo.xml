<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
    xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
    xsi:schemaLocation="http://www.springframework.org/schema/beans 
                        http://www.springframework.org/schema/beans/spring-beans.xsd
                        http://code.alibabatech.com/schema/dubbo 
                        http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="order-system" />
    <!-- 使用zookeeper注册中心暴露服务地址 -->
    <dubbo:registry address="zookeeper://${zk.connect}" />
    <!-- 用dubbo协议在20880端口暴露服务 -->
    <dubbo:protocol name="dubbo" port="20881" />
    <!-- 声明需要暴露的服务接口 -->
<!--     <dubbo:reference id="switchService"      interface="com.fangbaba.sw.service.SwitchService"             group="switch_${dubbo-group}" version="1.0.0" retries="0" check="false" timeout="10000"/>
    <dubbo:reference id="stockService"       interface="com.fangbaba.stock.face.service.IStockService"     group="stock_${dubbo-group}"  version="1.0.0" retries="0" check="false" timeout="10000"/>
    <dubbo:reference id="hotelOnsaleService" interface="com.fangbaba.gds.face.service.IHotelOnsaleService" group="gds_${dubbo-group}"    version="1.0.0" retries="0" check="false" timeout="10000"/>
    <dubbo:reference id="hotelService"       interface="com.fangbaba.basic.face.service.HotelService"      group="basic_${dubbo-group}"  version="1.0.0" retries="0" check="false" timeout="10000"/>
 -->    <dubbo:monitor protocol="registry"></dubbo:monitor>
</beans>